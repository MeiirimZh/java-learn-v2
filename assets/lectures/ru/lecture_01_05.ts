import { Lecture } from "../../types";

const description = `
В этой лекции вы узнаете, зачем нужен компилятор в Java, как он работает, 
какие ошибки может находить, а также познакомитесь с комментариями в коде: 
их видами, назначением и забавными примерами из жизни программистов.
`;

const content = `
# Компилятор и комментарии в Java

## Что такое компилятор

Компилятор — это программа, которая переводит текст программы, написанный на языке Java,  
в специальный код, который понимает компьютер. В Java компиляция проходит в **байт-код**, а не в машинные инструкции напрямую.

Java-компилятор преобразует каждый класс в отдельный файл \`.class\`, который содержит байт-код.  
Когда запускаешь программу, байт-код интерпретируется или компилируется в машинный код JVM (Java Virtual Machine). Это позволяет Java работать на разных устройствах и операционных системах без переделки кода под каждую платформу.

## Почему компилятор важен

- Он проверяет код на **синтаксические ошибки** до запуска программы.  
- Если где-то синтаксис неверный, компилятор укажет на это, и программа не запустится, пока ошибки не исправлены.  
- Он превращает код в удобный для JVM байт-код, который может быть выполнен на любой системе с JVM.

## Примеры ошибок компиляции

### Пропущена точка с запятой
\`\`\`
System.out.println("Привет") // ошибка компиляции
\`\`\`

### Неправильное имя переменной
\`\`\`
int number;
System.out.println(numbr); // не существует переменной numbr
\`\`\`

## Что такое комментарии
Комментарии — это части кода, которые пишутся для людей, но **игнорируются компилятором**.
Компилятор пропускает их — они не влияют на выполнение программы.

Есть два вида комментариев:
### 1) Строчные комментарии
Начинаются с \`//\` и действуют до конца строки:
\`\`\`
// Это комментарий
System.out.println("Hello"); // комментарий после кода
\`\`\`
### 2) Блочные комментарии
Открываются \`/*\` и закрываются \`*/\`. Могут занимать несколько строк:
\`\`\`
/*
  Это многострочный
  комментарий
*/
System.out.println("Hi!");
\`\`\`

## Зачем нужны комментарии
Комментарии помогают:
- Объяснить, что делает сложный участок кода.
- Напомнить себе или другим программистам о задумке.
- Временно отключать часть кода при отладке.

## Примеры забавных комментариев
В реальных проектах можно встретить разные комментарии, например:
\`\`\`
// TODO: исправить это позже
// FIXME: здесь баг!
// Удалить этот код, когда никто не смотрит
\`\`\`
Такие пометки помогают ориентироваться в коде.

## Итоги
- **Компилятор** — переводит твой Java-код в байт-код и проверяет на ошибки.
- **Комментарии** — текст, который читают люди, но компилятор пропускает мимо себя.
- Комментарии делают код понятнее, но они **не влияют на работу программы**.
`;

export const lecture_01_05: Lecture = {
    id: 13,
    title: "Компилятор и комментарии",
    course_id: 1,
    level: 1,
    number: 5,
    description: description,
    content: content
};