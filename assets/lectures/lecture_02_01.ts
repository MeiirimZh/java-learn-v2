import { Lecture } from "../../types";

const description = `
В этой лекции вы узнаете, как работать с целыми числами в Java с 
помощью типа int: диапазон значений, объявление переменных, 
присваивание, арифметические операции, деление, остаток от деления, 
инкремент и декремент. Примеры и пояснения помогут быстро освоить основы 
работы с целочисленными переменными.
`;

const content = `
# Целые числа: тип \`int\`, операции с типом \`int\`

## 1. Тип \`int\`

\`int\` — это **примитивный тип данных**, который хранит целые числа (без дробной части).  
Диапазон значений: от \`-2_147_483_648\` до \`2_147_483_647\`.

Примеры:
\`\`\`
int a = 10;
int b = -5;
int c = 0;
\`\`\`
- \`a\` хранит положительное число
- \`b\` — отрицательное
- \`c\` — ноль

## 2. Объявление переменной типа \`int\`
Чтобы создать переменную типа \`int\`:
\`\`\`
int number;
\`\`\`
Здесь переменная **number** объявлена, но значения ещё нет.
Можно сразу присвоить значение:
\`\`\`
int number = 15;
\`\`\`

## 3. Арифметические операции с \`int\`
Java поддерживает стандартные операции для целых чисел:
| Оператор | Действие           | Пример       |
| -------- | ------------------ | ------------ |
| \`+\`      | сложение           | \`5 + 3 = 8\`  |
| \`-\`      | вычитание          | \`5 - 3 = 2\`  |
| \`*\`      | умножение          | \`5 * 3 = 15\` |
| \`/\`      | деление (целое)    | \`5 / 2 = 2\`  |
| \`%\`      | остаток от деления | \`5 % 2 = 1\`  |
Пример в коде:
\`\`\`
int a = 10;
int b = 3;

System.out.println(a + b); // 13
System.out.println(a - b); // 7
System.out.println(a * b); // 30
System.out.println(a / b); // 3
System.out.println(a % b); // 1
\`\`\`
> ⚠️ При делении \`int\` / \`int\` дробная часть отбрасывается. Для дробного результата хотя бы один операнд должен быть double.

## 4. Приоритет операций
При выполнении выражений порядок такой:
1. \`*\`, \`/\`, \`%\`
2. \`+\`, \`-\`
Пример:
\`\`\`
int x = 2 + 3 * 4; // 14, сначала 3*4, потом +2
\`\`\`
Скобки изменяют порядок:
\`\`\`
int y = (2 + 3) * 4; // 20
\`\`\`

## 5. Изменение значения переменной
\`\`\`
int count = 5;
count = count + 3; // 8
\`\`\`
Сокращённая форма:
\`\`\`
count += 3; // то же самое
count -= 2; // уменьшить на 2
count *= 2; // умножить на 2
count /= 4; // разделить на 4
count %= 3; // остаток от деления на 3
\`\`\`

## 6. Инкремент и декремент
\`\`\`
int x = 5;
x++; // x = 6
x--; // x = 5
\`\`\`
Можно использовать **перед переменной** или **после**, с разницей в порядке вычисления в сложных выражениях.

## 7. Примеры задач
\`\`\`
int a = 7;
int b = 2;

System.out.println(a + b); // 9
System.out.println(a / b); // 3
System.out.println(a % b); // 1
\`\`\`
\`\`\`
int x = 10;
x += 5;   // x = 15
x--;      // x = 14
\`\`\`

## Итог
- \`int\` — тип для целых чисел.
- Операции: \`+\`, \`-\`, \`*\`, \`/\`, \`%\`.
- Деление \`int / int\` отбрасывает дробную часть.
- Можно изменять переменные с помощью \`=\`, \`+=\`, \`-=\`, \`*=\`, \`/=\`, \`%=\`.
- Инкремент \`++\` и декремент \`--\` — быстрый способ увеличить или уменьшить на 1.
`;

export const lecture_02_01: Lecture = {
    id: 16,
    title: "Целые числа: тип int, операции с типом int",
    course_id: 1,
    level: 2,
    number: 1,
    description: description,
    content: content
};