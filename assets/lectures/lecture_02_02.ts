import { Lecture } from "../../types";

const description = `
В этой лекции вы познакомитесь с типом String в Java: научитесь создавать и 
инициализировать строковые переменные, выполнять конкатенацию, экранировать 
спецсимволы, а также использовать базовые методы для работы с текстом. 
Практические примеры и пояснения помогут уверенно работать со строками в ваших программах.
`;

const content = `
# Строки и текст: тип \`String\`, работа со строками

## 1. Что такое \`String\`

\`String\` — это **тип данных для хранения текста**.  
Строки — это последовательность символов, заключённая в **двойные кавычки** \`" "\`.

Примеры:

\`\`\`
String name = "Вася";
String greeting = "Привет, мир!";
String empty = ""; // пустая строка
\`\`\`
> ⚠️ Одинарные кавычки ' ' используются только для одного символа (char), а не для текста.

## 2. Объявление и присвоение строк
Можно сначала объявить переменную:
\`\`\`
String text;
\`\`\`
А потом присвоить значение:
\`\`\`
text = "Java";
\`\`\`
Или сразу при объявлении:
\`\`\`
String message = "Добро пожаловать!";
\`\`\`

## 3. Конкатенация (склеивание строк)
В Java строки можно склеивать с помощью оператора \`+\`:
\`\`\`
String firstName = "Вася";
String lastName = "Петров";
String fullName = firstName + " " + lastName;
System.out.println(fullName); // Вася Петров
\`\`\`
Можно соединять **строки** и **числа**:
\`\`\`
int age = 18;
String text = "Возраст: " + age;
System.out.println(text); // Возраст: 18
\`\`\`

## 4. Длина строки
Чтобы узнать, сколько символов в строке, используем метод \`.length()\`:
\`\`\`
String s = "Hello";
System.out.println(s.length()); // 5
\`\`\`

## 5. Объединение нескольких строк
\`\`\`
String s1 = "Привет";
String s2 = "мир";
String s3 = s1 + ", " + s2 + "!";
System.out.println(s3); // Привет, мир!
\`\`\`

## 6. Экранирование символов
Некоторые символы требуют **специального обозначения**:
| Символ | Использование   |
| ------ | --------------- |
| \`\\"\`   | двойная кавычка |
| \`\\\\\`   | обратный слеш   |
| \`\\n\`   | перенос строки  |
| \`\\t\`   | табуляция       |
Пример:
\`\`\`
System.out.println("Он сказал: \\"Привет!\\"\\nНовая строка.");
\`\`\`
Вывод:
\`\`\`
Он сказал: "Привет!"
Новая строка.
\`\`\`

## 7. Строковые методы (основные)
\`\`\`
String text = "JavaRush";

// Проверка длины
int len = text.length(); // 8

// Преобразование в верхний/нижний регистр
String upper = text.toUpperCase(); // JAVARUSH
String lower = text.toLowerCase(); // javarush

// Получение символа по индексу
char c = text.charAt(0); // 'J'

// Сравнение строк
boolean equals = text.equals("JavaRush"); // true
boolean equalsIgnoreCase = text.equalsIgnoreCase("javarush"); // true

// Проверка начала/конца строки
boolean starts = text.startsWith("Java"); // true
boolean ends = text.endsWith("Rush"); // true
\`\`\`
> Индексы в строке начинаются с 0.

## 8. Пример программы со строками
\`\`\`
String firstName = "Иван";
String lastName = "Иванов";
int age = 20;

String info = "Имя: " + firstName + ", Фамилия: " + lastName + ", возраст: " + age;
System.out.println(info);
\`\`\`
Вывод:
\`\`\`
Имя: Иван, Фамилия: Иванов, возраст: 20
\`\`\`

## Итог
- \`String\` хранит текстовые данные.
- Для склеивания строк используется \`+\`.
- Основные методы: \`.length()\`, \`.toUpperCase()\`, \`.toLowerCase()\`, \`.charAt()\`, \`.equals()\`.
- Строки можно комбинировать с числами для удобного вывода.
- Для специальных символов применяются экранирующие последовательности (\`\\"\`, \`\\\\\`, \`\\n\`, \`\\t\`).
`;

export const lecture_02_02: Lecture = {
    id: 17,
    title: "Строки и текст: тип String, работа со строками",
    course_id: 1,
    level: 2,
    number: 2,
    description: description,
    content: content
};